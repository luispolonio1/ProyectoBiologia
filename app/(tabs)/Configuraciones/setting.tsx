import { Text, View } from 'react-native';

export default function Tab() {
  return (
    <View className='flex-1 justify-center items-center bg-slate-50'>
      <Text className='font-bold text-4xl'>Tab Settings</Text>
    </View>
  );
}